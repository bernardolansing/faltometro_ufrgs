meta {
  name: Update courses
  type: http
  seq: 3
}

post {
  url: {{ENDPOINT}}/admin/update-courses
  body: none
  auth: bearer
}

auth:bearer {
  token: {{BEARER_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
