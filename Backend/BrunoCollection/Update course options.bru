meta {
  name: Update course options
  type: http
  seq: 4
}

post {
  url: {{ENDPOINT}}/Admin/UpdateCourseOptions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{BEARER_TOKEN}}
}

body:json {
  {
    "ufrgs_session_id": "..."
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
