meta {
  name: Update course options
  type: http
  seq: 4
}

post {
  url: {{ENDPOINT}}/admin/update-course-options
  body: json
  auth: bearer
}

auth:bearer {
  token: {{BEARER_TOKEN}}
}

body:json {
  {
    "ufrgs_session_id": "..."
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
